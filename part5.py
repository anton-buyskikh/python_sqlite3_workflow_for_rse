#
# Part V: extra (UNFINISHED)
# 1. save any object in the table by creating a `connector`, e.g. numpy.array
# 2. for data version control save git_hash in the table
# 3. the database can be updated by one process at a time, hence while running
#    simulations on a cluster with multiple simultaneous jobs the waiting time
#    of the connection has to be increased to several minutes.
#
